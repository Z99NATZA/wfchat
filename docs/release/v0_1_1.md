# v0.1.1 - Risk Review Hardening

Status: Draft

Security, reliability, and documentation hardening release based on `docs/project-risk-review.md`.

Use this file as the completed-work log for v0.1.1 while `docs/project-risk-review.md` stays focused on unresolved risks.

## Closed Risk Items

| Area | Status | Notes |
| --- | --- | --- |
| Admin route protection | Done | `/api/admin/*` now requires a valid `UserKind::Admin` session. |
| Admin documentation drift | Done | Backend architecture docs now match runtime protection. |
| Stale JSON store docs/config | Done | PostgreSQL/`sqlx` is documented as current; unused `DATA_PATH` was removed from `.env.example`. |
| Transactional chat persistence | Done | Chat message append, attachment linking, and chat timestamp update now commit atomically. |
| Database error visibility | Done | Store operations now propagate PostgreSQL errors instead of silently returning success, not-found, false, or empty lists. |
| Voice latest-model compatibility | Done | VOICEVOX speech-text translation now works with latest OpenAI text models that reject custom temperature. |

## Security

### Admin Authorization

- Protected `/api/admin/*` routes with session and role authorization.
- Required `UserKind::Admin` for admin AI profile and provider status endpoints.
- Added backend route coverage for missing, guest, registered, and admin sessions.

Impact:

- Missing, guest, and registered sessions now receive `403 Forbidden`.
- Admin sessions can access admin profile/status data.
- Admin API callers must send a session belonging to `UserKind::Admin`.

## Reliability

### Transactional Chat Persistence

- Wrapped chat message append, pending attachment linking, and chat timestamp update in one PostgreSQL transaction.
- Stopped ignoring write errors in the chat append path.
- Kept pending attachments unlinked and rolled back inserted messages when requested attachment linking is incomplete.
- Added backend regression coverage for the rollback behavior.

Impact:

- A failed attachment link can no longer leave newly inserted user/assistant messages behind.
- Valid pending attachments remain pending if the full append operation cannot commit.

### Database Error Visibility

- Changed backend store methods to return explicit PostgreSQL `Result` values for session, auth/profile, chat, attachment, memory, and sync operations.
- Preserved expected not-found behavior as `404 Not Found` while letting real database failures become `500 database error`.
- Added database error logging through the API error boundary.
- Logged stale pending attachment cleanup failures instead of treating cleanup query failure as an empty cleanup batch.
- Updated backend route and integration tests for the new store error contract.

Impact:

- Failed database writes are no longer reported as optimistic API success.
- Failed reads no longer look like empty chat, memory, attachment, or sync state.
- Sync commits no longer treat failed upserts or commit-log writes as ordinary skipped items.

## Compatibility

### Voice Text Model Compatibility

- Shared the OpenAI text-model temperature compatibility rule between normal chat requests and VOICEVOX `japanese_translation` speech-text generation.
- Kept `temperature: 0.2` for translation models that support it.
- Omitted `temperature` for `gpt-5.5` and `gpt-5.5-*` translation requests.
- Kept text, TTS, and transcription model configuration documented as separate endpoint capabilities.
- Added backend regression coverage for the voice translation request payload.

Impact:

- `OPENAI_MODEL=gpt-5.5` can be used for main chat text and speech-text translation without breaking assistant voice playback.
- `AI_VOICE_MODEL` and `AI_TRANSCRIPTION_MODEL` remain audio-specific model ids.

## Documentation

- Updated backend architecture docs to state that admin endpoints require an admin session.
- Updated backend architecture docs to describe PostgreSQL and `sqlx` as the current persistence layer.
- Updated chat image attachment docs to state that message persistence and attachment linking commit atomically.
- Updated chat voice and Docker docs to distinguish text, TTS, and transcription model settings.
- Added behavior history for the transactional chat append decision.
- Added behavior history for latest text model compatibility in speech-text translation.
- Added behavior history for explicit backend database error propagation.
- Updated the admin role description to reflect current admin endpoint access.
- Removed stale `DATA_PATH` configuration from `apps/api/.env.example`.
- Removed completed admin authorization items from the active project risk review.
- Removed completed stale JSON-store documentation/config items from the active project risk review.
- Removed completed multi-step database write transaction risk from the active project risk review.
- Removed completed database error visibility risk from the active project risk review.
- Renumbered remaining active risk items.

## Verification

Commands run:

```powershell
cargo fmt --manifest-path apps/api/Cargo.toml --check
cargo test --manifest-path apps/api/Cargo.toml
```

Result:

- Backend tests: 92 passed

## Upgrade Notes

- No database migration is required.
- No frontend change is required.
